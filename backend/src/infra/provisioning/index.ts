/**
 * Provisioning adapter — execa wrappers for terraform + ansible, workdir
 * management. Stub for Phase 0. Implemented in Phase 5 (see ARCHITECTURE.md,
 * PLAN.md, SECURITY.md — terraform/ansible only ever run from the worker).
 */
export {};
